#! /bin/bash

set -e

if [[ "$#" -ne "1" ]]; then echo "Requires device name (e.g. nvme2n2) argument" && exit; fi

DATA_DIR=data

QUEUE_DEPTHS=(1 2 4 8 16 32 64 128 256 512 1024)

mkdir -p $DATA_DIR
echo $DEV_CAP

for depth in ${QUEUE_DEPTHS[@]}; do
    echo $depth
    # Run each benchmark another for each bench type
done
